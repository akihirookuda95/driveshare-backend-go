package main

func main() {

	/*
		// Numeric types session
		var (
			u8  uint8     = 255    // 0 to 255
			i8  int8      = 127    // -128 to 127
			u16 uint16    = 65535  // 0 to 65535
			c64 complex64 = 1 + 2i // 0 to 65535
		)
		fmt.Println(u8, i8, u16, c64)

		fmt.Printf("type=%T value=%v\n", u8, u8) // type=uint8 value=255

	*/

	/*
		// Basic calculation session
		fmt.Println("1 + 1 = ", 1+1)   // 2
		fmt.Println("10 - 1 = ", 10-1) // 9
		fmt.Println("10 * 2 = ", 10*2) // 20
		fmt.Println("10 / 5 = ", 10/5) // 2
		fmt.Println("10 / 3 = ", 10/3) // 3
		fmt.Println("10 % 2 = ", 10%2) // 0
		fmt.Println("10 % 3 = ", 10%3) // 1

		// Code style session
		fmt.Println(1 + 1)    // 2, single calculation has space
		fmt.Println(1+1, 1+2) // 2 3, multiple calculation do not have space

	*/

	/*
		// Increment and decrement session
		x := 0
		fmt.Println(x) // 0
		x++            // increment
		fmt.Println(x) // 1
		x--            // decrement
		fmt.Println(x) // 0
	*/

	/*
		// Bitwise operation session
		fmt.Println(1 << 0) // 0001
		fmt.Println(1 << 1) // 0010
		fmt.Println(1 << 2) // 0100
		fmt.Println(1 << 3) // 1000

	*/

}
